<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USER</title>
    <style>
        table,
        th,
        tr,
        td {
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <h1>Display of all data:</h1>
    <table>
        <th>User</th>
        <th>id</th>
        <th>email</th>
        <% for(user of users){ %>
            <tr>
                <td><%=user.username%></td>
                <td><%=user.id%></td>
                <td><%=user.email%></td>
                <td>
                    <form action="/user/<%= user.id%>/edit" method="get">
                        <button>Edit username</button>
                    </form>
                </td>
                <td>
                    <form action="/user/<%= user.id%>/deleteform" method="get">
                        <button>DELETE username</button>
                    </form>
                </td>
            </tr>
        <% }%>
        
    </table>
</body>
</html>
